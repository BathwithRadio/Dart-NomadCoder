void main() {
  //use var when possible
  var giveMeFive = true;
  var numbers = [
    1,
    2,
    3,
    4,
    //collection if
    if (giveMeFive) 5,
    /*
    same with
    if (giveMeFice) {
      numbers.add(5);
    }
    */
  ];
  // List<int> numbers2 = [1, 2, 3, 4];

  print(numbers);

  //support collection if, for
}
